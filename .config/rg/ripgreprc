# For reference see official guide:
# https://github.com/BurntSushi/ripgrep/blob/master/GUIDE.md#configuration-file

# Limit max line length, and show a preview
--max-columns=150
--max-columns-preview

# Display line number
--line-number

# Show column as well
#--column

# Include hidden files
--hidden

# Include/exclude files and folders:
#  - exclude .git/
--glob=!.git/*

# Set the colors
#  - options: never | auto (default) | always | ansi
#  - `auto` will cause ripgrep to only enable colors when it is printing to a
#    terminal, if its output is piped to a file or process, colors will be
#    suppressed
--color=auto

# Because who cares about case!?
--smart-case

